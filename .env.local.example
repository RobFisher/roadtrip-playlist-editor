# Frontend + backend shared public client IDs
VITE_GOOGLE_CLIENT_ID=your_google_oauth_web_client_id
VITE_SPOTIFY_CLIENT_ID=your_spotify_app_client_id

# Frontend Spotify OAuth callback (optional; defaults to current page URL)
VITE_SPOTIFY_REDIRECT_URI=http://127.0.0.1:5173/

# Frontend backend API base URL (optional)
# Leave unset for local Vite proxy mode (/api -> http://127.0.0.1:8787)
# VITE_API_BASE_URL=https://your-api-id.execute-api.<region>.amazonaws.com

# Backend API CORS allowed origins for CDK deploys (comma-separated, no trailing slash)
# Keep local origin and add deployed frontend origin(s).
# BACKEND_CORS_ALLOWED_ORIGINS=http://127.0.0.1:5173,https://d231ej2mp8aqcu.cloudfront.net

# Optional Vite local proxy override
# VITE_LOCAL_API_PROXY_TARGET=http://127.0.0.1:8787

# Optional local backend host/port overrides
# LOCAL_API_HOST=127.0.0.1
# LOCAL_API_PORT=8787
